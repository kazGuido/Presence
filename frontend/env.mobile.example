# Mobile / Capacitor: copy to `.env.mobile` and run `vite build --config vite.config.mobile.ts`
# Production API origin (no trailing slash). Required when the app is not served from the API host.
# VITE_API_URL=https://presence.nicedaytech.com
